{{ $.Scratch.Add "items" slice }}

{{ $counter := 0 }}
{{- range .Site.RegularPages -}}
  {{ if in .Permalink "/post/" }}
  {{ $counter = add $counter 1 }}
  {{- $.Scratch.Add "items" (dict "id" $counter "title" .Title "body" .Plain "url" .Permalink) -}}
  {{ end }}
{{- end -}}

{{ $.Scratch.Get "items" | jsonify }}
